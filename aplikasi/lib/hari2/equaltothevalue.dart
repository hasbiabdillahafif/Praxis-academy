import 'dart:html';
import 'dart:typed_data';

index_equal_value(T, st, end)
if st>End
  then return -1
  mid <-(st+end)/ 2
  if T[mid]> Mid
  then return Index_equal_value(T, st, mid-1)
  else if T[mid] <Mid
then return index_equal_value(T, mid +end)
else return mid
